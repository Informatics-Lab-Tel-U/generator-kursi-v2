version: "3.8"

services:
    app:
        build:
            context: .
            target: runtime
        container_name: kursi-generator-app
        ports:
            - "4323:4321"
        environment:
            - NODE_ENV=production
        env_file:
            - .env
        restart: unless-stopped
